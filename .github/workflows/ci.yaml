# Nome exibido na aba Actions do GitHub.
name: CI - Java, Docker e Amazon ECR

# Situações que iniciam automaticamente o workflow.
on:

  # Executa ao enviar commits para a master ou para a branch de implementação.
  push:
    branches:
      - master
      - feature/github-actions-ecr

  # Executa quando uma Pull Request apontar para a master.
  pull_request:
    branches:
      - master

# Permissões concedidas ao workflow.
permissions:

  # Permite baixar o código do repositório.
  contents: read

  # Permite que o GitHub gere um token OIDC.
  # Esse token será trocado por credenciais temporárias da AWS.
  id-token: write

# Valores reutilizados em diferentes passos do workflow.
env:
  AWS_REGION: sa-east-1
  AWS_ROLE_ARN: arn:aws:iam::033649548808:role/GitHubActionsEcrRecomecoRole
  ECR_REPOSITORY: recomeco/pagamento-service

jobs:

  build:

    # Máquina Linux temporária fornecida pelo GitHub.
    runs-on: ubuntu-latest

    steps:

      # Baixa o pagamento-service para a máquina temporária.
      - name: Baixar código
        uses: actions/checkout@v6

      # Instala o Java 21 e habilita o cache das dependências Maven.
      - name: Configurar Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"
          cache: maven

      # No Linux, o Maven Wrapper precisa ter permissão de execução.
      - name: Preparar Maven Wrapper
        run: chmod +x mvnw

      # Compila, executa os testes e gera o arquivo JAR.
      - name: Testar e gerar pacote
        run: ./mvnw clean verify

      # Constrói a imagem usando a versão mais recente da imagem-base.
      #
      # github.sha identifica a imagem local com o commit exato
      # que iniciou esta execução.
      - name: Construir imagem Docker
        run: docker build --pull --tag pagamento-service:${{ github.sha }} .

      # Os passos seguintes executam somente em um push para a master.
      # Uma Pull Request apenas testa e constrói a imagem.
      - name: Obter credenciais temporárias da AWS
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          role-session-name: pagamento-service-${{ github.run_id }}
          aws-region: ${{ env.AWS_REGION }}

      # Usa as credenciais temporárias para autenticar o Docker no ECR.
      - name: Autenticar Docker no Amazon ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # Adiciona duas tags e envia a imagem:
      #
      # latest:
      # aponta para a versão mais recente da master.
      #
      # sha-<commit>:
      # identifica exatamente o código que produziu a imagem.
      - name: Publicar imagem no Amazon ECR
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          IMAGE_URI="${ECR_REGISTRY}/${ECR_REPOSITORY}"

          docker tag pagamento-service:${COMMIT_SHA} "${IMAGE_URI}:latest"
          docker tag pagamento-service:${COMMIT_SHA} "${IMAGE_URI}:sha-${COMMIT_SHA}"

          docker push "${IMAGE_URI}:latest"
          docker push "${IMAGE_URI}:sha-${COMMIT_SHA}"